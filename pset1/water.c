#include <cs50.h>
#include <stdio.h>


int main(void)
{
      int count_boutles;
      printf("the number of minutes spent in the shower ");
      scanf("%d", &count_boutles);
      printf("bouttles : %d\n", count_boutles * 12);
}